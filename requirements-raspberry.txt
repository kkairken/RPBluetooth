# Python dependencies for Raspberry Pi
# Optimized for Raspberry Pi 3/4/5 with Raspberry Pi OS

# ============================================================================
# CORE DEPENDENCIES
# ============================================================================

# Numerical computing
numpy>=1.23.0,<2.0.0

# Computer vision
# NOTE: On Raspberry Pi, prefer python3-opencv from apt for better performance
# If installing via pip:
opencv-python>=4.8.0

# Image processing
Pillow>=10.0.0

# ONNX Runtime for face recognition
# NOTE: On Raspberry Pi 3, use the CPU version
onnxruntime>=1.15.0

# Configuration files
PyYAML>=6.0

# ============================================================================
# BLE SUPPORT (for real BLE GATT server)
# ============================================================================

# DBus Python bindings (for BlueZ communication)
# IMPORTANT: This is difficult to compile via pip
# RECOMMENDED: Install system package instead:
#   sudo apt install python3-dbus
#
# If you must use pip (requires system dependencies):
#   sudo apt install libdbus-1-dev libdbus-glib-1-dev
dbus-python>=1.3.2

# Python GObject bindings (for GLib mainloop)
# IMPORTANT: This is difficult to compile via pip
# RECOMMENDED: Install system package instead:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
#
# If you must use pip (requires system dependencies):
#   sudo apt install libgirepository1.0-dev libcairo2-dev
PyGObject>=3.42.0

# BLE Client library (for ble_register_client.py)
# This is easier to install via pip
bleak>=0.20.0

# ============================================================================
# GPIO SUPPORT (Raspberry Pi hardware control)
# ============================================================================

# Modern GPIO interface using libgpiod
# IMPORTANT: Install system package first:
#   sudo apt install python3-libgpiod libgpiod2 gpiod
#
# If you must use pip (requires libgpiod-dev):
#   sudo apt install libgpiod-dev
# libgpiod>=2.0.0

# ============================================================================
# TESTING
# ============================================================================

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# MediaPipe for advanced face detection
# WARNING: Large package, may be slow on Raspberry Pi 3
# mediapipe>=0.10.0

# Blue Zero (alternative BLE library)
# bluezero>=0.7.0

# ============================================================================
# DEVELOPMENT TOOLS (optional)
# ============================================================================

# Code formatting
# black>=23.0.0
# flake8>=6.0.0

# Type checking
# mypy>=1.0.0
