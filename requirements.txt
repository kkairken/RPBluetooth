# Core dependencies
numpy>=1.23.0,<2.0.0
opencv-python>=4.8.0
Pillow>=10.0.0

# ONNX Runtime for face recognition
onnxruntime>=1.15.0

# Configuration
PyYAML>=6.0

# BLE GATT Server support (for real BLE server on Raspberry Pi)
# NOTE: dbus-python and PyGObject are difficult to install via pip
# RECOMMENDED: Use system packages instead:
#   sudo apt install python3-dbus python3-gi
# If you must use pip (requires system dependencies):
dbus-python>=1.3.2
PyGObject>=3.42.0

# BLE Client support (optional - for ble_register_client.py)
bleak>=0.20.0

# GPIO support (Raspberry Pi only) - using libgpiod (modern interface)
# RECOMMENDED: Use system package:
#   sudo apt install python3-libgpiod
# If you must use pip (requires libgpiod-dev):
# libgpiod>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: MediaPipe for face detection
# mediapipe>=0.10.0
